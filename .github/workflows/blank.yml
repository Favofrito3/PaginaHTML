name: CICDFabianBuitron

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Desplegar página HTML
        run: echo "Despliegue exitoso de la página HTML"
